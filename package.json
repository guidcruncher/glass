{
  "name": "liquid-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run format && npm run registry && vite --host",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "registry": "cd ./scripts && node ./generateGlassRegistry.ts",
    "sass": "npm run registry && mkdir -p ./dist && sass ./src/scss/style.scss ./dist/style.css",
    "sass-migrate": "sass-migrator calc-interpolation  --migrate-deps ./src/scss/styles.scss && sass-migrator color --migrate-deps ./src/scss/styles.scss && sass-migrator division --migrate-deps ./src/scss/styles.scss && sass-migrator module --migrate-deps ./src/scss/styles.scss && sass-migrator namespace --migrate-deps ./src/scss/styles.scss && sass-migrator strict-unary --migrate-deps ./src/scss/styles.scss",
    "format": "prettier --config ./.prettierrc --write src/",
    "stylelint": "stylelint **/*.scss"
  },
  "dependencies": {
    "prettier-plugin-organize-imports": "^4.3.0",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.8.1",
    "prettier": "^3.6.2",
    "sass": "^1.93.2",
    "stylelint": "^16.25.0",
    "stylelint-config-standard": "^39.0.1",
    "stylelint-config-standard-scss": "^16.0.0",
    "stylelint-prettier": "^5.0.3",
    "typescript": "~5.9.3",
    "vite": "^7.1.7",
    "vue-tsc": "^3.1.0"
  }
}
